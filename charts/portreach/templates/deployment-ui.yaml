{{- if .Values.ui.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "portreach.ui.fullname" . }}
  labels:
    {{- include "portreach.ui.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.ui.replicas }}
  revisionHistoryLimit: {{ .Values.ui.revisionHistoryLimit }}
  minReadySeconds: {{ .Values.ui.minReadySeconds }}
  strategy:
    {{- toYaml .Values.ui.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "portreach.ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "portreach.ui.selectorLabels" . | nindent 8 }}
        {{- with .Values.ui.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- $podAnnotations := merge (.Values.ui.podAnnotations | default dict) (.Values.commonAnnotations | default dict) }}
      {{- with include "portreach.agent.token.checksum" . }}
      {{- $_ := set $podAnnotations "checksum/agent-token" . }}
      {{- end }}
      {{- with $podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "portreach.ui.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccounts.ui.automountServiceAccountToken }}
      terminationGracePeriodSeconds: {{ .Values.ui.terminationGracePeriodSeconds }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.ui.podSecurityContext | nindent 8 }}
      {{- with .Values.ui.initContainers }}
      initContainers:
        {{- tpl (toYaml .) $ | nindent 8 }}
      {{- end }}
      containers:
        - name: ui
          image: {{ include "portreach.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - ui
            - --listen
            - ":{{ .Values.ui.port }}"
            - --timeout
            - {{ .Values.ui.timeout | quote }}
            {{- if eq (include "portreach.auth.enabled" .) "true" }}
            - --auth-config
            - /etc/portreach/auth/auth.yaml
            {{- end }}
            {{- with .Values.ui.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.ui.port }}
              protocol: TCP
          env:
            - name: PORTREACH_AGENTS_DNS
              value: {{ include "portreach.agent.dnsName" . | quote }}
            - name: PORTREACH_AGENT_PORT
              value: {{ .Values.agent.port | quote }}
            {{- $branding := .Values.ui.branding | default dict }}
            {{- if and (hasKey $branding "title") (not (kindIs "invalid" $branding.title)) }}
            - name: PORTREACH_UI_TITLE
              value: {{ $branding.title | quote }}
            {{- end }}
            {{- if and (hasKey $branding "description") (not (kindIs "invalid" $branding.description)) }}
            - name: PORTREACH_UI_DESCRIPTION
              value: {{ $branding.description | quote }}
            {{- end }}
            {{- if and (hasKey $branding "footer") (not (kindIs "invalid" $branding.footer)) }}
            - name: PORTREACH_UI_FOOTER
              value: {{ $branding.footer | quote }}
            {{- end }}
            {{- $loginBranding := .Values.ui.loginBranding | default dict }}
            {{- if and (hasKey $loginBranding "title") (not (kindIs "invalid" $loginBranding.title)) }}
            - name: PORTREACH_LOGIN_TITLE
              value: {{ $loginBranding.title | quote }}
            {{- end }}
            {{- if and (hasKey $loginBranding "header") (not (kindIs "invalid" $loginBranding.header)) }}
            - name: PORTREACH_LOGIN_HEADER
              value: {{ $loginBranding.header | quote }}
            {{- end }}
            {{- if and (hasKey $loginBranding "footer") (not (kindIs "invalid" $loginBranding.footer)) }}
            - name: PORTREACH_LOGIN_FOOTER
              value: {{ $loginBranding.footer | quote }}
            {{- end }}
            {{- if eq (include "portreach.auth.browser.enabled" .) "true" }}
            {{- $secretName := include "portreach.auth.secretName" . }}
            - name: {{ .Values.ui.auth.cookieKeyEnv }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: {{ .Values.ui.auth.cookieKeySecretKey }}
            {{- range .Values.ui.auth.providers }}
            - name: {{ include "portreach.auth.providerSecretEnv" (dict "provider" .) }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: {{ include "portreach.auth.providerSecretKey" (dict "provider" .) }}
            {{- end }}
            {{- end }}
            {{- if eq (include "portreach.agent.token.enabled" .) "true" }}
            - name: PORTREACH_AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "portreach.agent.token.secretName" . }}
                  key: {{ .Values.agent.auth.tokenSecretKey }}
            {{- end }}
            {{- with .Values.ui.extraEnv }}
            {{- tpl (toYaml .) $ | nindent 12 }}
            {{- end }}
          {{- with .Values.ui.envFrom }}
          envFrom:
            {{- tpl (toYaml .) $ | nindent 12 }}
          {{- end }}
          {{- with include "portreach.probe" .Values.ui.probes.startup }}
          startupProbe:
            {{- . | nindent 12 }}
          {{- end }}
          {{- with include "portreach.probe" .Values.ui.probes.liveness }}
          livenessProbe:
            {{- . | nindent 12 }}
          {{- end }}
          {{- with include "portreach.probe" .Values.ui.probes.readiness }}
          readinessProbe:
            {{- . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.ui.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.ui.containerSecurityContext | nindent 12 }}
          {{- if or (eq (include "portreach.auth.enabled" .) "true") .Values.ui.extraVolumeMounts }}
          volumeMounts:
            {{- if eq (include "portreach.auth.enabled" .) "true" }}
            - name: auth-config
              mountPath: /etc/portreach/auth
              readOnly: true
            {{- end }}
            {{- with .Values.ui.extraVolumeMounts }}
            {{- tpl (toYaml .) $ | nindent 12 }}
            {{- end }}
          {{- end }}
        {{- with .Values.ui.sidecars }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      {{- if or (eq (include "portreach.auth.enabled" .) "true") .Values.ui.extraVolumes }}
      volumes:
        {{- if eq (include "portreach.auth.enabled" .) "true" }}
        - name: auth-config
          configMap:
            name: {{ include "portreach.ui.fullname" . }}-auth
        {{- end }}
        {{- with .Values.ui.extraVolumes }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.ui.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.ui.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- tpl (toYaml .) $ | nindent 8 }}
      {{- end }}
{{- end }}
